# 03 — Revisión de coherencia: lo que se dice vs. lo que se hace

Alcance: README.md, docs/DOCUMENTO_TECNICO.md, docs/IA_IMPLEMENTACION_Y_DESPLIEGUE.md, docs/AI_DISCLOSURE.md, docs/ANALISIS_BRECHAS.md, AGENTS.md, CLAUDE.md, docs/proceso/bitacora.md, tools/kb-rag/README.md, docs/knowledge-base/00-MOC.md, docs/Architecture.png y la UI (frontend/src), contrastados con backend/, ai-service/, etl-bancs/, frontend/, docker-compose.yml y docker/.

Qué verifiqué ejecutando:
- `npx jest` en backend: 2/2 pruebas unitarias pasan.
- ETL: `python etl_bancs_processor.py` sobre una copia regenera exactamente las 9 transacciones de `bancs_cleaned_features.json` (9/12).
- Preflight CORS reproducido con la misma configuración de `main.ts`: la respuesta **no** incluye `idempotency-key` (ver D1).
- Enlaces relativos de todos los .md: todos resuelven. No hay `file:///` ni rutas de Windows. Los wikilinks resuelven, salvo el ejemplo `[[Nombre de nota]]` de AGENTS/CLAUDE, que es un placeholder intencional.
- La prueba de integración (`npm run test:int`) no la ejecuté porque aquí no hay PostgreSQL.

---

## Severidad ALTA (afirmación falsa o instrucción/función rota)

| ID | Archivo:línea | Afirmación / problema | Qué hace realmente el código | Corrección concreta |
|---|---|---|---|---|
| D1 | README.md:28; ANALISIS_BRECHAS.md:120 ("Corregida en API y frontend"); AI_DISCLOSURE.md:222 | "Header `Idempotency-Key`… en API y frontend". Además la demo de transferencia desde la UI **no funciona**. | `frontend/src/services/api.ts:3,36` llama cross-origin (`:3000` → `http://localhost:4000`) con el header `Idempotency-Key`. Pero `backend/src/main.ts:364` permite solo `Content-Type, Accept, Authorization, x-correlation-id`. Reproducido: el preflight OPTIONS devuelve `Access-Control-Allow-Headers: Content-Type, Accept, Authorization, x-correlation-id`, así que el navegador bloquea el POST y el modal de transferencia queda con "Failed to fetch". | En `main.ts:364` usar `allowedHeaders: 'Content-Type, Accept, Authorization, x-correlation-id, Idempotency-Key'` y añadir `exposedHeaders: 'x-correlation-id'`. Probarlo en navegador antes del video. |
| D2 | .env.example:9; DOCUMENTO_TECNICO.md:82 | Modelo de IA: `.env.example` trae `GEMINI_MODEL=gemini-1.5-flash` y el doc dice "`gemini-1.5-flash` / `gemini-2.0-flash`". | `docker-compose.yml:94` y `ai-service/advisor.py:21` usan `gemini-2.5-flash`, igual que la UI (`App.tsx:122`, `AIAdvisorFeed.tsx:75`). Si alguien copia `.env.example` a `.env`, compose toma `gemini-1.5-flash`: es un modelo retirado y además `thinkingConfig` (`advisor.py:108`) no aplica, así que Gemini falla y **todo** cae al fallback en silencio. | `.env.example:9` → `GEMINI_MODEL=gemini-2.5-flash`. DOC:82 → "consume el modelo configurado en `GEMINI_MODEL` (por defecto `gemini-2.5-flash`)…" |
| D3 | README.md:47-99 (E3) | Las instrucciones no alcanzan para alguien sin contexto. | (a) Prerrequisitos: solo Docker Desktop, pero `npm test`/`npm run test:int` necesitan Node.js 20 y npm, y el ETL necesita Python 3.11 + pandas/numpy. (b) El ETL no dice `pip install -r etl-bancs/requirements.txt`. (c) No explica `GEMINI_API_KEY`: no menciona `.env.example`, ni `cp .env.example .env`, ni que sin clave funciona con el motor heurístico, ni `python ai-service/scripts/check_gemini.py`. (d) No hay prueba funcional de humo (curl de una transferencia). | Agregar a Prerrequisitos: "Node.js 20 + npm (solo para pruebas), Python 3.11 (solo para el ETL)". Agregar un paso 0: `cp .env.example .env` y, opcional, poner `GEMINI_API_KEY` (sin clave la IA usa el motor heurístico local). ETL: `pip install -r requirements.txt` antes de ejecutar. Añadir un ejemplo `curl -X POST localhost:4000/api/v1/transactions -H 'Content-Type: application/json' -H 'Idempotency-Key: demo-1' -d '{"sourceAccountNumber":"1000000001","targetAccountNumber":"1000000002","amount":25}'`. |
| D4 | DOCUMENTO_TECNICO.md:63 ("solo las marca como publicadas si el broker las aceptó"); IA_IMPL:37 | Garantía de publicación del outbox. | `rabbitmq.service.ts:88` usa `channel.publish` sobre un canal normal, sin *publisher confirms* (`createConfirmChannel`). `true` solo indica que el mensaje entró al buffer local, no que el broker lo aceptó. | Implementar `createConfirmChannel()` + `waitForConfirms()` o suavizar el texto: "las marca como publicadas cuando el cliente AMQP acepta el mensaje (sin *publisher confirms* en el MVP; en producción se activarían)". |
| D5 | README.md:27,75; DOCUMENTO_TECNICO.md:63,176; IA_IMPL:76 | "Si RabbitMQ está caído los eventos quedan en el outbox y **se publican al recuperarse**" (verificado por la prueba). | (a) La prueba `test/concurrency.int-spec.ts:172-178` usa un **stub** de RabbitMQ, no un broker real. (b) `rabbitmq.service.ts:35-77` hace 5 intentos cada 3 s y después **deja de reintentar para siempre** (al arrancar y tras un `close`). Si el broker tarda más de ~15 s en volver, el relay devuelve `false` indefinidamente hasta reiniciar el backend. En compose, `depends_on` no espera a que RabbitMQ esté sano (`docker-compose.yml:76-78`). | Reintento indefinido con backoff en `connectWithRetry`, más un healthcheck de rabbitmq con `condition: service_healthy`. En los docs, decir "verificado con un broker simulado (stub)". |
| D6 | docs/Architecture.png (imagen inicial del README) | Diagrama principal de la arquitectura. | (a) Se ve renderizado el aviso de PlantUML "Please use CSS style instead of skinparam padding". (b) Muestra "Pipeline ETL → PostgreSQL (Carga estructurada)", pero el ETL escribe un JSON (`etl_bancs_processor.py:156`) y no toca la BD. (c) Muestra Backend → RabbitMQ directo, sin `outbox_events` ni relay, que es justamente la corrección G1. | Regenerar sin `skinparam padding`. Dibujar Backend → PostgreSQL (`outbox_events`) → Relay → RabbitMQ, y ETL → `bancs_cleaned_features.json`. Hacer lo mismo con el ASCII de DOCUMENTO_TECNICO.md:12-43. |
| D7 | DOCUMENTO_TECNICO.md:92 | "El endpoint… procesa el débito/crédito (~15 ms), **emite un evento a RabbitMQ** y retorna inmediatamente". | Contradice la sección 2.1 del mismo documento y el código (`transactions.service.ts:195-206`): la petición no toca RabbitMQ; inserta en `outbox_events` y responde tras el COMMIT. | "…procesa el débito/crédito, inserta los eventos en `outbox_events` en la misma transacción y responde `201` tras el COMMIT; el relay publica en `smartbancs.ai.queue` fuera del camino crítico." |
| D8 | frontend/src/App.tsx:146 | UI: "Despacho **fire-and-forget** a RabbitMQ; respuesta en < 20 ms". | Es lo opuesto al diseño defendido (IA_IMPL:76 dice "Por qué así y no fire-and-forget"). El < 20 ms no está medido. | "Transactional Outbox: el evento se guarda en la misma transacción y se publica fuera del camino crítico." |
| D9 | frontend/src/components/OperationsIncidentConsole.tsx:112, :147 | Consola de incidente: "100% Consistencia ACID" y "Deadlocks Detectados: **0**". | Son **literales fijos**: no dependen del resultado (`simulation.service.ts:91-103` no devuelve deadlocks). Si la simulación falla, la UI sigue mostrando 0 deadlocks. Lo mismo `simulation.service.ts:129-130`, que devuelve siempre `healthStatus: 'HEALTHY'` y "Sin deadlocks activos". | Calcular en el backend (p. ej. el delta de `smartbancs_deadlocks_detected_total` o contar errores 40P01/55P03 en `results.errors`) y mostrarlo. Si no, eliminar esas tarjetas. Derivar `healthStatus` de `locks.some(l => !l.granted)`. |
| D10 | App.tsx:122; AIAdvisorFeed.tsx:75 | "Motor: Google Gemini 2.5 Flash" en cada recomendación. | Es fijo. `advisor.py` devuelve `engine` (Gemini o Heuristic), pero el backend lo descarta: no hay columna en `recommendation.entity.ts`. Sin API key, todas las recomendaciones vienen del motor heurístico y la UI dice Gemini. | Guardar `engine` en `metadata` y mostrarlo; o poner "Motor: Gemini 2.5 Flash o heurístico local (fallback)". |
| D11 | DOCUMENTO_TECNICO.md:88 | Fallback ante "timeouts (>4.5s)". | `advisor.py:118` usa `timeout=10.0` y el log dice ">10s" (`:142`). | "timeouts (> 10 s)". |
| D12 | IA_IMPLEMENTACION_Y_DESPLIEGUE.md:139 | "`/model-info` reporta en tiempo real la versión activa (`v2.4.0-smartbancs`)… y estado del drift (`NORMAL`)". | `advisor.py:18`: `v2.5.0-gemini-hybrid`; `main.py:11` declara la versión FastAPI `2.4.0`. `dataDriftStatus: "NORMAL"` está **fijo** (`main.py:68`): no se calcula drift. | "`/model-info` expone la versión (`v2.5.0-gemini-hybrid`) y contadores de inferencia; el estado de drift es un placeholder, el cálculo de PSI está diseñado (sección 2.2), no implementado." Alinear `main.py:11`. |
| D13 | DOCUMENTO_TECNICO.md:96-98 | "El modelo **se alimenta dinámicamente** con… `bancs_cleaned_features.json`"; "**Se evalúa** … PSI. Desviaciones con `confidenceScore < 0.50` desvían la operación a revisión manual"; "contenedor con límites de memoria y CPU… control de cuotas RPM". | Nada en `ai-service/` lee el JSON (grep: solo lo menciona el propio ETL). No hay PSI, ni umbral 0.50 (y `/model-info` dice `confidenceThreshold: 0.85`, `main.py:69`), ni revisión manual, ni límites de recursos en `docker-compose.yml:82-99`, ni control de RPM. | Pasar la sección 3.3 a condicional o diseño: "En producción el modelo se alimentaría…", "se evaluaría PSI…". Unificar el umbral (0.85 o 0.50). Opcional: añadir `deploy.resources.limits` al servicio `ai-service` en compose para que sea cierto. |
| D14 | IA_IMPLEMENTACION_Y_DESPLIEGUE.md:118-119, :149-161 | "El pipeline ETL diario ([`etl_bancs_processor.py`]) extrae… Bancs y SmartBancs… **anonimizadas** (PCI-DSS y GDPR)… frecuencia semanal"; "Configuración estricta en Docker/K8s"; "Perfiles… **cacheadas** en memoria… inferencias en < 5 ms". | El script enlazado lee un CSV local y no anonimiza: los números de cuenta salen en claro en `bancs_cleaned_features.json`. No genera "frecuencia semanal" (solo `isHighValue`, `logAmount`, `channelRiskScore`, `:110-119`). No hay límites de recursos en compose ni caché en `advisor.py`. | Separar "Implementado en el MVP" de "Diseño para producción". Quitar "anonimizadas" y "frecuencia semanal" del script, o implementarlos (p. ej. hash SHA-256 de cuenta). Quitar "cacheadas… < 5 ms" o marcarlo como propuesta. |
| D15 | docs/AI_DISCLOSURE.md:174-178 vs docs/proceso/bitacora.md:17-19, CLAUDE.md:5-6, AGENTS.md:3 | La tabla de herramientas **omite OpenCode**. | La bitácora y CLAUDE.md asignan a OpenCode (con un modelo barato o local) tareas mecánicas y la generación de la evidencia e5 (prompt OC-1), y la evidencia existe con `backend: e5`. | Añadir fila: "OpenCode (modelo local/barato) · agente de tareas mecánicas · formato de notas, ejecución de `smoke_test.py` con e5 en la PC del autor". |
| D16 | AI_DISCLOSURE.md:219, :224 vs bitacora.md:10-11 y el frontmatter de las 14 notas | "Se investigaron fuentes primarias… y se redactaron 12 notas"; "Aporte del candidato: … **selección de fuentes y revisión de las notas**". | La bitácora dice "Investigación: Claude (búsqueda web)" y "Notas de referencia: Claude". Las **14** notas de la bóveda siguen en `estado: borrador`, y el propio MOC (00-MOC.md:46) dice "Ninguna nota se considera defendible hasta estar `revisada`". Es autoría humana atribuida a trabajo de la IA, y la revisión declarada no consta. | O el autor revisa y pasa las notas a `estado: revisada` antes de entregar, o se reescribe: "Claude investigó y redactó las 12 notas; el candidato definió el checklist, aprobó las fuentes y revisó [N] notas". |
| D17 | AI_DISCLOSURE.md:217, 00-MOC.md:8, AGENTS.md:17-23 | Narrativa del proceso contradictoria. | MOC: la bóveda "se construyó **antes** de escribir código". AI_DISCLOSURE 2.8: "**Con la primera versión del MVP ya funcional**, se aplicó…". AGENTS.md: "No escribas código de la aplicación hasta la fase 4", aunque el MVP ya existía. Fechas: `bitacora.md:5,14` = 2026-09-22; `bancs_cleaned_features.json` `executedAt` = 2026-09-22T03:46Z; evidencia RAG = 2026-09-22. | Elegir la versión real y usarla en los tres archivos. La de AI_DISCLOSURE (MVP v1 con Gemini/Antigravity, luego KB + RAG + brechas con Claude) es la coherente con ANALISIS_BRECHAS. Corregir MOC:8 a "se construyó para auditar el MVP contra referencias". Ver también M-riesgo de fechas (D30). |
| D18 | AI_DISCLOSURE.md:222 ("un commit por brecha"); ANALISIS_BRECHAS.md:113 vs :119-124 | "Mejoras (un commit por brecha)". | La tabla de estado de ANALISIS_BRECHAS atribuye G1, G2, G3 y G6 a **un solo** commit `fix(G1,G2,G3,G6)`. El jurado puede comprobarlo con `git log`. | "Mejoras (commits referenciados por ID de brecha; G1–G3 y G6 en un mismo commit por estar acoplados en `transactions.service.ts`)". |
| D19 | AI_DISCLOSURE.md:236 vs :178, :222 | La auditoría (sección 3, punto 5) dice que las brechas "fueron **corregidas por el candidato**". | La tabla (línea 178) y la sección 2.8 dicen que Claude Code hizo "implementación de las correcciones y de la prueba de concurrencia". Además, las brechas citadas en el punto 5 (observabilidad teórica, post-mortem, disclosure) no son G1–G10. | "…fueron validadas por el candidato e implementadas con Claude Code (ver 2.8)". Distinguir las dos auditorías: la de Opus sobre la documentación y la de Claude Code + RAG sobre el código. |

## Severidad MEDIA (no sustentado, desactualizado o confuso)

| ID | Archivo:línea | Afirmación / problema | Qué dice realmente el código | Corrección concreta |
|---|---|---|---|---|
| D20 | README.md:26; DOCUMENTO_TECNICO.md:51, :92, :129; AIAdvisorFeed.tsx:79; App.tsx:146; IA_IMPL:161; advisor.py:151 | Cifras de rendimiento: "~15-20 ms", "< 20 ms", "~15 ms", "< 5ms", "< 5 ms", "< 1ms". **NO SUSTENTADAS**: no hay prueba de carga, captura ni evidencia en `04-Evidencias/`. | La latencia se mide (`smartbancs_transaction_duration_seconds`, `execution_time_ms`, respuesta de `/simulation/quincena-spike`), pero no hay ningún resultado versionado. | Sustituir por "latencia medida en `smartbancs_transaction_duration_seconds`; en la simulación local de 100 tx concurrentes el p95 fue X ms (ver `docs/evidencias/simulacion-quincena.png`)", o quitar el número. |
| D21 | DOCUMENTO_TECNICO.md:88; IA_IMPL:12, :85 | "100% de disponibilidad", "100% desacoplado", "Zero-Downtime". **NO SUSTENTADO** y de tono marketing. | El fallback evita que la IA falle, pero el consumidor hace `ack` incluso cuando el backend rechaza la recomendación (`consumer.py:40-45`): la recomendación se pierde en silencio. | "La transacción no depende de la IA; si Gemini falla, la recomendación la produce el motor heurístico." Quitar "100%" y "Zero-Downtime". |
| D22 | DOCUMENTO_TECNICO.md:105; README.md:112 | "Logs Estructurados (JSON)", "Winston Logger estructurado (JSON + CorrID)". | `logger.service.ts:18-27`: el transport de consola reemplaza el formato por `printf` y emite texto `[ts] [LEVEL] [service] [CorrID: x] msg {meta}`, no JSON. `docker compose logs backend` no muestra JSON. | Quitar el `format` del transport (así queda `winston.format.json()`) o escribir "logs semiestructurados (texto + metadatos JSON) con correlationId". |
| D23 | DOCUMENTO_TECNICO.md:53 | Stack de observabilidad: "Pino/Winston". | No hay Pino en `package.json`. | "Winston". |
| D24 | DOCUMENTO_TECNICO.md:136-137 | Alertas `HighTransactionLatency` y `SLAViolation`: `histogram_quantile(0.95, smartbancs_transaction_duration_seconds)`. | PromQL inválido: `histogram_quantile` necesita `rate(..._bucket[5m])` agrupado por `le` (el dashboard sí lo hace bien: `smartbancs_dashboard.json`). Un jurado SRE lo nota. Además no hay `rules` en `prometheus.yml`. | `histogram_quantile(0.95, sum by (le) (rate(smartbancs_transaction_duration_seconds_bucket[5m]))) > 1`. Aclarar "(propuestas, no configuradas en el MVP)". |
| D25 | DOCUMENTO_TECNICO.md:108 | Buckets "5ms, 10ms, 20ms, 50ms, 100ms, 500ms, 1s, 2s". | `metrics.service.ts:54`: incluye además 200 ms. | Añadir 200ms. |
| D26 | DOCUMENTO_TECNICO.md:172 | "`40P01` y `40001` se reintentan **hasta 3 veces**"; solo `55P03`/`57014` devuelven 503. | `TX_MAX_ATTEMPTS=3` son 3 **intentos**, es decir 2 reintentos (`transactions.service.ts:87`; el log dice "retry 1/2"). Agotados, los 40P01/40001 también devuelven 503 (`:109`). | "…se reintentan hasta 2 veces (3 intentos en total) … si se agotan, 503". |
| D27 | DOCUMENTO_TECNICO.md:163, :233 | Fragmento `const accountsToLock = [...].sort();` presentado como código. | `transactions.service.ts:140`: `const [firstAccNum, secondAccNum] = [sourceAccountNumber, targetAccountNumber].sort();`. | Copiar la línea real con su número. |
| D28 | DOCUMENTO_TECNICO.md:166, :235; App.tsx:141; OperationsIncidentConsole.tsx:55 | "Elimina **matemáticamente** la posibilidad de un deadlock". | El propio código reintenta 40P01 y tiene métrica de deadlocks. El orden solo cubre las filas de `accounts` en esta ruta. La afirmación absoluta invita a la pregunta "¿entonces para qué el reintento?". | "Elimina el ciclo de espera entre dos transferencias sobre las mismas cuentas; el reintento por 40P01 cubre rutas no previstas (defensa en profundidad)." |
| D29 | DOCUMENTO_TECNICO.md:200-247 | La ficha del post-mortem (INC-202609-001, MTTD 2 min, ~350 fallidas, ~2 000 usuarios, TPS 3 500) se lee como un incidente real. Pool "25/25" frente a `DB_POOL_MAX: 30` (`docker-compose.yml:67`). La causa raíz ("locks sin orden determinista") y el "fix aplicado" no ocurrieron: el orden ya existía en el MVP v1 (ANALISIS_BRECHAS.md:24). | — | Encabezar con "Post-mortem ilustrativo del incidente simulado: cifras hipotéticas". Usar pool 30/30. Nombre de alerta coherente: 4.2 usa `ConnectionPoolExhaustion` y 6.5 `ConnectionPoolSaturation`. Ruta completa `POST /api/v1/simulation/quincena-spike` (6.5). |
| D30 | bitacora.md:5,14; retrieval-smoke-test.md:3; bancs_cleaned_features.json (`executedAt`) | Todas las fechas del proceso son **2026-09-22**, y el reto (reto-original.md:29) fija la entrega el "domingo 20 a las 23:00" (el 20-sep-2026 fue domingo). | — | Confirmar el plazo real. Si la entrega era el 20-sep, el jurado verá trabajo posterior. Explicarlo en README o bitácora, o no dejar fechas que lo contradigan. |
| D31 | bitacora.md:19, :21-23; AGENTS.md:18 / CLAUDE.md:25 | "Evidencia con e5: **Pendiente**"; "falta generar la evidencia con e5". La bitácora termina en la fase 2. | La evidencia existe (`04-Evidencias/retrieval-smoke-test.md`, `backend: e5`, hit@5 37/41). Las fases 3-5 (brechas, correcciones, revisión) no están en la bitácora aunque AI_DISCLOSURE las describe. | Actualizar la bitácora con las fases 3-5 (herramienta, fecha, commit) y marcar OC-1 como hecho. Actualizar el estado de fases en AGENTS.md y CLAUDE.md. |
| D32 | AGENTS.md:15, :30; 00-MOC.md:39-40; CLAUDE.md:28 | "ADR en `03-Decisiones/`"; "docs/proceso/ … prompts (OpenCode, fase 3)". | `03-Decisiones/` solo tiene `.gitkeep`. No existe ningún archivo de prompts en `docs/proceso/`. | Escribir 2–3 ADR cortos (Outbox vs publicación directa; lock pesimista ordenado vs optimista; RabbitMQ vs Kafka), que además sirven para E1/RNF-5. O quitar las menciones. |
| D33 | ANALISIS_BRECHAS.md:18-45 | La tabla "Resumen por requisito" es la **foto previa** a las correcciones: R3.1e ⚠️ "sin prueba automatizada", R3.2 "declarado pero no implementado", R3.5a ❌, RNF-3 "despacho sin await". Las referencias `archivo:línea` (`transactions.service.ts:50-70`, `:132`, `:141`, `:162`; `app.module.ts:39-46`) apuntan a código que ya no existe en esas líneas. | El estado real está en la sección 4. | Titular la sección 1 "Estado antes de las correcciones (commit `<sha>`)" y añadir una columna "Estado actual". Si no, el jurado lee ❌ primero. |
| D34 | ANALISIS_BRECHAS.md:33 | "Métricas… latencia de IA". | `smartbancs_ai_recommendation_duration_seconds` se declara (`metrics.service.ts:71`) pero **nunca** se observa, y ai-service no expone `/metrics`. | Quitar "latencia de IA" o instrumentarla (en `recommendations.service.create`, o un `/metrics` en FastAPI). |
| D35 | AI_DISCLOSURE.md:174 vs :176, :190 | ¿Quién generó el código base? Una fila dice que Gemini/Antigravity hizo "generación de código base"; otra, que Claude Haiku hizo "scaffolding de módulos, DTOs, entidades…". | — | Precisar qué hizo cada herramienta y en qué fase (v1 con Antigravity; Haiku ¿cuándo?). |
| D36 | AI_DISCLOSURE.md:189 | "Diseño de la base de datos (autoría del candidato)… tablas `accounts`, `transactions` y `ai_recommendations`… La IA se usó solo para revisar". | `outbox_events`, `idempotency_key` y los índices únicos parciales (`schema.sql:46-52, 74-98`) salieron del análisis de brechas implementado con Claude Code (2.8). | Añadir: "Las extensiones `outbox_events` e `idempotency_key` se incorporaron en la fase de corrección (G1, G2) con Claude Code." |
| D37 | README.md:21-43, DOCUMENTO_TECNICO.md:65-66 | "Change Data Capture… **se implementa** un conector CDC". | No existe. | "se implementaría (diseño)". |
| D38 | IA_IMPL:87 | "Worker **Concurrente** con Prefetch". | `pika.BlockingConnection` con un solo hilo: procesa en serie. `prefetch_count=5` solo limita los mensajes en vuelo. | "Worker con `prefetch_count=5` (procesamiento secuencial; se escala con réplicas)". |
| D39 | IA_IMPL:1.2 (líneas 50-74) | Fragmentos de código presentados como literales. | Difieren del real (`outbox-relay.service.ts:279-295` también actualiza `attempts`/`last_error`). | Indicar "(fragmento simplificado)". |
| D40 | backend/.env.example:9,10 | `DB_SYNCHRONIZE=true`, `DB_PASSWORD=postgres`. | G6 se "corrigió" a `false` (`app.module.ts:42`, compose:66). La contraseña del Postgres del compose es `postgrespassword`: con este `.env`, `npm run start:dev` falla al autenticarse. | `DB_SYNCHRONIZE=false`, `DB_PASSWORD=postgrespassword`. Quitar `AI_SERVICE_URL`, que el backend no usa (grep sin resultados). |
| D41 | .env.example (raíz):11-26 | Define `DB_*`, `RABBITMQ_URL`, etc. | `docker-compose.yml` solo lee `GEMINI_API_KEY` y `GEMINI_MODEL` del `.env`; el resto está fijo en el compose. Da una falsa sensación de que se configuran ahí. | Dejar solo `GEMINI_API_KEY` y `GEMINI_MODEL`, o comentar "(informativo; compose usa valores fijos)". |
| D42 | ETLViewer.tsx:7-22; AI_DISCLOSURE.md:203 ("comparador visual ETL Raw vs. Cleaned") | El comparador muestra datos **fijos en el código**, no lee `bancs_cleaned_features.json`. | — | Decirlo ("muestra de ejemplo") o servir el JSON como asset estático. |

## Severidad BAJA (estilo e higiene)

| ID | Archivo:línea | Problema | Corrección |
|---|---|---|---|
| D43 | backend/package.json:18 | Script `test:e2e` apunta a `./test/jest-e2e.json`, que no existe. | Eliminar el script. |
| D44 | README.md:68-74 | `docker compose up -d postgres  # (desde la raíz)` va después de `cd backend`; se lee como si hubiera que ejecutarlo dentro de backend. | Poner primero `docker compose up -d postgres` en la raíz y luego `cd backend && npm install && npm test && npm run test:int`. |
| D45 | README.md:102-134 | La estructura del repo omite `modules/outbox/`, `test/`, `tools/kb-rag/`, `docs/knowledge-base/`, `docs/reto/`, `docs/proceso/` y los otros docs. | Actualizar el árbol. |
| D46 | ANALISIS_BRECHAS.md:52 | "el documento técnico y **el guion** afirman…": referencia a un archivo borrado. | "el documento técnico afirmaba…". |
| D47 | ANALISIS_BRECHAS.md:47-105 | Orden G1…G8, G10, G9. | Reordenar. |
| D48 | AI_DISCLOSURE.md:222 | La lista de mejoras omite G10 (métrica del pool). | Añadir G10. |
| D49 | DOCUMENTO_TECNICO.md:5 | "Versión 1.0.0 (Producción MVP)". | "Versión 1.0 (MVP)". |
| D50 | DOCUMENTO_TECNICO.md:49 | "Clean Architecture". | "arquitectura modular por dominio (módulos NestJS)". |
| D51 | DOCUMENTO_TECNICO.md:181 | "`pg_locks` muestra qué **filas**… están bloqueadas". | La consulta (`simulation.service.ts:117-121`) lista locks de relación; los locks de fila no aparecen en `pg_locks` salvo en espera. Mejor: "qué relaciones tienen locks y cuáles no están concedidos". |
| D52 | DOCUMENTO_TECNICO.md:72 | "estandariza fechas… ISO-8601 **UTC**". | `standardize_datetime` no convierte zona horaria, solo añade `Z`. Escribir "asume UTC". |
| D53 | etl `FOOD_ENTERTAINMENT` (etl:25) | No existe en el enum del backend (`schema.sql:11`) ni en las categorías de la IA (`main.py:67`). | Mapear a `FOOD` o documentar que el ETL es un dataset independiente. |
| D54 | AI_DISCLOSURE.md:206 | "configuraciones de health checks" (plural). | Solo postgres tiene healthcheck. |
| D55 | tools/kb-rag/README.md:70-76 | La tabla "Archivos" omite `lint_vault.py`. | Añadir la fila. |
| D56 | advisor.py:68-76 | `system_instruction` definido y nunca enviado. | Enviarlo como `systemInstruction` o borrarlo. |
| D57 | Idioma | Logs del backend en inglés; `help` de métricas mezclado (en/es, `metrics.service.ts`); UI y docs en español; mensajes del ETL sin tildes. | Unificar (p. ej. código y logs en inglés, docs en español) y mencionarlo en README. |
| D58 | Tono y emojis | Emojis en todos los H2 del README y en `IA_IMPL` (título, actores Mermaid). Marketing: "misión crítica", "alta resiliencia", "Self-Healing JSON", "Zero-Downtime", "100%". | Quitar emojis de encabezados y los superlativos. El jurado técnico valora la precisión. |
| D59 | backend/package.json:5 | `"author": "SmartBancs Engineering Team"`. | Poner el nombre del candidato. |

---

## Entregables faltantes (E1–E6)

| ID | Estado | Detalle |
|---|---|---|
| E1 Documento técnico | Presente, con correcciones | DOCUMENTO_TECNICO.md cubre arquitectura, Bancs, IA, observabilidad, incidente y post-mortem. Hay que corregir D6-D7, D11, D13, D20-D29 y D37. RNF-5 pide justificar el stack por **rendimiento, seguridad y escalabilidad**: la tabla 1.2 casi no trata seguridad (no hay authN/Z, las credenciales están en compose y CORS es `*`). Falta un párrafo de seguridad (lo que el MVP no hace y cómo se haría). |
| E2 Repositorio | Presente | Código, scripts, configuraciones y pruebas (2 unitarias + 4 de integración). |
| E3 Instrucciones | **Incompleto** | Ver D3 (prerrequisitos Node y Python, `.env` y `GEMINI_API_KEY`, `pip install`, smoke test con curl), D44 y D40-D41. |
| E4 Evidencias | **Falta casi todo** | (1) **Video demostrativo**: no existe ni hay enlace. (2) **Material de presentación**: no existe; el guion de 3 min se borró y no hay slides ni PDF. (3) **Datos de prueba**: existen (`backend/sql/seed.sql`, `etl-bancs/bancs_raw_transactions.csv`, `bancs_cleaned_features.json`, cuentas del test de integración), pero el README no los presenta como "datos de prueba utilizados". Faltan además capturas o resultados de la simulación de quincena, de Grafana y de `npm run test:int`, que sustentarían D20. ANALISIS_BRECHAS G9 lo marca "Pendiente". |
| E5 Declaración de IA | Presente, con inconsistencias | Ver D15-D19, D35-D36. |
| E6 Defensa | Fuera del repo | No hay material de apoyo para los 3 + 4 minutos (se borró el guion). Recomendación: `docs/presentacion/` con slides en PDF y enlace al video en el README (sección "Evidencias"). |

Sección sugerida para el README:
```
## Evidencias
- Video demostrativo: <enlace>
- Presentación: docs/presentacion/SmartBancs.pdf
- Datos de prueba: backend/sql/seed.sql (5 cuentas), etl-bancs/bancs_raw_transactions.csv (12 registros con nulos, duplicado y formatos mixtos) → bancs_cleaned_features.json (9 válidos)
- Resultados: docs/knowledge-base/04-Evidencias/ (RAG hit@5, test:int, simulación de quincena, Grafana)
```

---

## Higiene del repo

Lo que se versiona: 119 archivos. No hay `dist/`, `__pycache__`, `.env`, bundles, `.vscode` ni `.idea`. El `.gitignore` es correcto.

| Archivo | Recomendación |
|---|---|
| `Reto_Tecnico_NextGen_Enginner Final.docx` (raíz) | **Quitar.** Es el enunciado del cliente, ya extraído literalmente a `docs/reto/reto-original.md`. Un binario con espacios en el nombre no aporta, y re-publicar el reto de TCS puede no estar permitido. Añadir `*.docx` al `.gitignore`. |
| `docs/Architecture.png` | Regenerar (D6): tiene el warning de PlantUML visible y contradice el código. |
| `etl-bancs/bancs_cleaned_features.json` | Mantener (sirve de dato de prueba o evidencia), pero decirlo en el README y aclarar que el script lo regenera. |
| `AGENTS.md` / `CLAUDE.md` | **Opinión:** refuerzan la historia del proceso (división de trabajo entre modelos, reglas anti-alucinación como "nunca inventes URLs" o "solo el autor pasa a revisada"), y eso es un punto fuerte ante un jurado que evalúa el uso de IA. Pero tal como están **confunden**: estado de fases obsoleto (D31), la regla "no escribas código hasta la fase 4" contradice que el MVP ya existía (D17), y CLAUDE.md duplica íntegro AGENTS.md. Propuesta: dejar AGENTS.md actualizado con el estado final, reducir CLAUDE.md a "Ver AGENTS.md" más la división de trabajo, y enlazar ambos desde AI_DISCLOSURE como "instrucciones dadas a los agentes". |
| `docs/proceso/bitacora.md` | Mantener y **completar** (D31). Hoy termina en la fase 2 con "pendientes" que ya están hechos. |
| `docs/knowledge-base/**` (14 notas en `borrador`) | Mantener, porque demuestra el método. Pero el propio MOC dice que nada es defendible en `borrador` (D16). Revisarlas y marcarlas `revisada`, o cambiar la regla del MOC. |
| `03-Decisiones/.gitkeep`, `04-Evidencias/.gitkeep` | Carpeta de ADR vacía (D32). Borrar el `.gitkeep` de 04, que ya tiene contenido. |
| Estilo | Emojis y superlativos (D58). Idioma mezclado (D57). `author` genérico (D59). |
